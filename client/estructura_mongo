{
  _id: ObjectId,
  nombre: String,
  correo: String,
  contraseña: String,
  esAdmin: Boolean,
  listasFavoritas: [ObjectId], // Referencia a las listas marcadas como favoritas por el usuario
}

{
  _id: ObjectId,
  nombre: String,
  descripcion: String,
  peliculas: [{
    _id: ObjectId,
    titulo: String,
    director: String,
    anio: Number,
    // Otros campos de información de la película
  }],
  autor: ObjectId, // Referencia al usuario que creó el listado
  likes: [{
    usuario: ObjectId, // Referencia al usuario que dio like
    fecha: Date // Fecha en la que se dio el like
  }],
  seguidores: [ObjectId], // Referencia a los usuarios que siguen el listado
  comentarios: [{
    autor: ObjectId, // Referencia al usuario que hizo el comentario
    contenido: String, // Contenido del comentario
    fecha: Date // Fecha en la que se hizo el comentario
  }]
}
